# GhostDebugger — Python dependencies
# Pinned for reproducibility in Docker

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# LLM — OpenAI-compatible client works with Fireworks AI
openai==1.35.3
# Pinned: openai 1.35.3's internal client init passes `proxies=` to httpx.Client,
# which httpx>=0.28 removed. Without this pin, `pip install` resolves the newest
# httpx and the backend crashes on startup with:
#   TypeError: Client.__init__() got an unexpected keyword argument 'proxies'
httpx==0.27.2

# Frontend
streamlit==1.35.0
requests==2.32.3

# Utilities
python-dotenv==1.0.1
